all:
  children:
    proxmoxmain:   # Centre Node, usually the first node in the cluster
      hosts:
        pve01:
          ansible_host: 10.200.8.22
          ansible_user: ansible
    # proxmoxmember:  # Member Nodes, other nodes in the cluster
    #   hosts: 
    #     pve02:
    #       ansible_host: 10.200.5.202
    #       ansible_user: ansible-proxmox
    #     pve03:
    #       ansible_host: 10.200.5.203
    #       ansible_user: ansible-proxmox


# Fill more node if have 